# Approach 1: Docker Compose
Spins up separate containers (mini virtual machines) that talk over a network.
Best for application-level vulnerability testing. User needs to describe _what_ they want, not _how_ to do it. 